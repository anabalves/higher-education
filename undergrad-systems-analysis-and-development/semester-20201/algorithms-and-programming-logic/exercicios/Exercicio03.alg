algoritmo "Exercicio03"

var

   base, altura, area : real

inicio

   escreva("Digite a base do triangulo: ")
   leia(base)
   escreva("Digite a altura do triangulo: ")
   leia(altura)
   
   area <- (base * altura) / 2
   
   escreva("A area do triangulo e igual a: ", area)

fimalgoritmo